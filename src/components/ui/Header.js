import React from 'react';
import { AppBar, Toolbar } from '@mui/material';



const Header = (props) => {
    return (
        <AppBar>
            <Toolbar>
                dApps Development
            </Toolbar>
        </AppBar>
    );
}

export default Header;